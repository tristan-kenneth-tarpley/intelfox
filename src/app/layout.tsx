import type { Metadata } from 'next';
import { ClerkProvider } from '@clerk/nextjs';
import { Inter } from 'next/font/google';
import './globals.css';
import classNames from 'classnames';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'IntelFox',
  description: 'Generated by create next app',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <ClerkProvider>
      <html lang="en">
        <body className={inter.className}>
          {/* hack to make sure that dynamically-generated button classes work */}
          <div className={classNames(
            'hidden',
            'text-zinc-200',
            'bg-red-600 bg-orange-600 bg-cyan-600',
            'text-red-600 text-orange-600 text-cyan-600',
            'border-red-600 border-orange-600 border-cyan-600',
            'ring-red-600 ring-orange-600 ring-cyan-600',
            'hover:bg-red-200 hover:bg-orange-200 hover:bg-cyan-200',
            'hover:bg-red-400 hover:bg-orange-400 hover:bg-cyan-400',
          )} />
          {children}
        </body>
      </html>
    </ClerkProvider>
  );
}
